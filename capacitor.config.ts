import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Task-manager project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
